#include <stdio.h>
#include <string.h>
int main() {
char name[100]="Nahid";
int sl= strlen(name);
 /*
 for(int i0;name[i]!='\0';i++){count++;}
int i=0;
while(name[i] != '\0'){
    count++;    i++;
}
 */

printf("%d",sl);
 
 return 0;
}